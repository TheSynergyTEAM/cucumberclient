import { Row } from 'antd'
import styled from 'styled-components'

export const StyledChatContainer = styled.div`
  height: 100vh;
  width: 100%;
  overflow: hidden;
`

export const StyledChatMain = styled(Row)`
  height: 100%;
`
